pub struct Player {
    pub game_id: String,
    pub player_id: String,
    pub username: String,
}

pub struct CreatePlayerInput {
    pub game_id: String,
    pub player_id: String,
    pub username: String,
}
